---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: previewconfigs.preview.homelab.io
spec:
  group: preview.homelab.io
  names:
    kind: PreviewConfig
    listKind: PreviewConfigList
    plural: previewconfigs
    shortNames:
    - pc
    singular: previewconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.deploymentType
      name: Type
      type: string
    - jsonPath: .spec.redis.enabled
      name: Redis
      type: boolean
    - jsonPath: .spec.storage.enabled
      name: Storage
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: PreviewConfig defines the preview requirements for an app
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PreviewConfigSpec defines the desired preview configuration
            properties:
              configVolume:
                description: ConfigVolume defines a config PVC to clone with automatic
                  URL rewriting
                properties:
                  claimName:
                    description: ClaimName is the PVC name in the production namespace
                      to snapshot
                    type: string
                  snapshotClass:
                    default: ceph-block-snapshot
                    description: SnapshotClass is the VolumeSnapshotClass to use
                    type: string
                required:
                - claimName
                type: object
              deploymentType:
                allOf:
                - enum:
                  - deployment
                  - helm
                - enum:
                  - deployment
                  - helm
                default: deployment
                description: DeploymentType specifies how the app is deployed
                type: string
              envMapping:
                description: |-
                  EnvMapping defines how to inject preview values into Deployments
                  Used when DeploymentType is "deployment"
                properties:
                  appUrl:
                    description: App URL (for OIDC redirect, etc.)
                    type: string
                  containerNames:
                    description: |-
                      ContainerNames lists the container names to patch with env overrides.
                      If empty, defaults to the app name.
                    items:
                      type: string
                    type: array
                  databaseHost:
                    description: Database configuration
                    type: string
                  databaseName:
                    type: string
                  databasePassword:
                    type: string
                  databaseUser:
                    type: string
                  oidcClientId:
                    description: OIDC configuration
                    type: string
                  oidcClientSecret:
                    type: string
                  oidcRedirectUri:
                    type: string
                  redisHost:
                    description: Redis configuration
                    type: string
                  redisPort:
                    type: string
                  s3AccessKey:
                    type: string
                  s3Bucket:
                    type: string
                  s3Endpoint:
                    description: S3/Storage configuration
                    type: string
                  s3Region:
                    type: string
                  s3SecretKey:
                    type: string
                type: object
              helmValues:
                description: |-
                  HelmValues defines paths to values for HelmRelease patching
                  Used when DeploymentType is "helm"
                properties:
                  appUrl:
                    description: App URL
                    type: string
                  databaseHost:
                    description: Database configuration
                    type: string
                  databaseName:
                    type: string
                  databasePassword:
                    type: string
                  databaseUser:
                    type: string
                  oidcClientId:
                    description: OIDC configuration
                    type: string
                  oidcClientSecret:
                    type: string
                  redisHost:
                    description: Redis configuration
                    type: string
                  redisPort:
                    type: string
                  s3AccessKey:
                    type: string
                  s3Bucket:
                    type: string
                  s3Endpoint:
                    description: S3/Storage configuration
                    type: string
                  s3SecretKey:
                    type: string
                type: object
              redis:
                description: Redis configuration for preview
                properties:
                  enabled:
                    description: Enabled specifies whether the app needs Redis
                    type: boolean
                required:
                - enabled
                type: object
              sharedServices:
                description: |-
                  SharedServices lists services that should use production instances
                  e.g., ["litellm"] means preview uses production litellm
                items:
                  type: string
                type: array
              storage:
                description: Storage configuration for preview (S3/object storage)
                properties:
                  bucket:
                    description: Bucket is the bucket name to use/create
                    type: string
                  enabled:
                    description: Enabled specifies whether the app needs S3-compatible
                      storage
                    type: boolean
                required:
                - enabled
                type: object
            type: object
          status:
            description: PreviewConfigStatus defines the observed state of PreviewConfig
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastProcessedTime:
                description: LastProcessedTime is when the config was last processed
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
