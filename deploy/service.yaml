apiVersion: v1
kind: Service
metadata:
  name: preview-operator-metrics
  namespace: preview-system
  labels:
    app.kubernetes.io/name: preview-operator
    app.kubernetes.io/component: manager
spec:
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
      protocol: TCP
  selector:
    app.kubernetes.io/name: preview-operator
